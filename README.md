# Суточный рапорт - Локальная веб-форма

Одностраничное приложение для заполнения формы суточного рапорта с функциями предварительного просмотра, автосохранения и экспорта в PDF.

## Реализованные возможности

- ✅ **Многошаговый интерфейс** - удобная навигация по шагам заполнения
- ✅ **Валидация полей** - проверка обязательных полей и корректности данных
- ✅ **Автосохранение** - автоматическое сохранение изменений с задержкой
- ✅ **Управление черновиками** - сохранение, восстановление и очистка черновиков
- ✅ **Предварительный просмотр** - точное отображение итогового документа
- ✅ **Экспорт в PDF** - генерация PDF-файла высокого качества
- ✅ **Экспорт в Word** - генерация DOCX-файла
- ✅ **Загрузка файлов** - прикрепление изображений и документов
- ✅ **Локальное хранение** - данные остаются на устройстве пользователя
- ✅ **Адаптивный дизайн** - работает на различных устройствах
- ✅ **Печать** - оптимизированные стили для печати
- ✅ **Формирование полного отчета** - объединение отчетов от всех подразделений
- ✅ **Ретроспектива аварийных ремонтов** - просмотр всех аварийных ситуаций за выбранный период
- ✅ **Система аутентификации** - разграничение доступа для разных подразделений
- ✅ **Тестовые данные** - готовые отчеты для тестирования функциональности

## Установка и запуск

### Системные требования

- Современный веб-браузер (Chrome, Edge, Firefox, Safari)
- Операционная система: Windows, macOS, Linux
- Не менее 100 МБ свободного места на диске

### Установка

1. Скачайте все файлы проекта в одну папку
2. Убедитесь, что все файлы находятся в одной директории:
   - `index.html` - главная страница приложения
   - `styles.css` - стили оформления
   - `app.js` - основная логика приложения
   - `storageAdapter.js` - адаптер для работы с локальным хранилищем
   - Папка `test_reports` с тестовыми отчетами
   - Папка `test_reports_for_month` с тестовыми отчетами за месяц

### Запуск приложения

1. Двойным кликом откройте файл `index.html`
2. Или перетащите файл в окно браузера Chrome
3. Приложение готово к использованию

## Используемые технологии

### Фронтенд

- **HTML5** - структура веб-страниц
- **CSS3** - стилизация и адаптивный дизайн
- **JavaScript (ES6+)** - интерактивность и логика приложения
- **LocalStorage/IndexedDB** - локальное хранение данных
- **File API** - работа с файлами пользователя

### Внешние библиотеки

- **html2pdf.js** - генерация PDF-документов
- **JSZip** - создание ZIP-архивов с тестовыми данными

### Архитектура

- **Одностраничное приложение (SPA)** - все взаимодействие происходит в рамках одной HTML-страницы
- **Модульная структура** - код разделен на логические модули (app.js, storageAdapter.js)
- **Событийная модель** - реакция на действия пользователя через систему событий
- **Адаптивный дизайн** - корректное отображение на устройствах с разными размерами экранов

## Структура проекта

```
Суточный рапорт/
├── index.html          # Основная HTML-страница
├── styles.css          # Стили оформления
├── app.js              # Главная логика приложения
├── storageAdapter.js   # Адаптер для работы с локальным хранилищем
├── update.html         # Страница с информацией о версии
├── checklist.html      # Чек-лист проверки программы
├── credentials.html    # Страница с учетными данными
├── test_reports/       # Тестовые отчеты для каждого подразделения
│   ├── index.html      # Страница для тестирования отчетов
│   ├── test_report_tec.json  # Тестовый отчет для Теплоэлектроцентрали
│   ├── test_report_zsv.json  # Тестовый отчет для Стоковых вод
│   ├── test_report_pcx.json  # Тестовый отчет для Паросилового хозяйства
│   └── test_report_erc.json  # Тестовый отчет для Электроремонтного цеха
├── test_reports_for_month/   # Тестовые отчеты за месяц
│   ├── test_report_tec_01.json ... test_report_tec_07.json
│   ├── test_report_zsv_01.json ... test_report_zsv_07.json
│   ├── test_report_pcx_01.json ... test_report_pcx_07.json
│   └── test_report_erc_01.json ... test_report_erc_07.json
└── README.md           # Данная инструкция
```

## Формы отчетов

Приложение предоставляет возможность заполнения отчетов для следующих подразделений:

1. **Отчет о работе Теплоэлектроцентрали за сутки**
2. **Отчет о работе участка по забору, подготовке и очистке сточных вод за сутки**
3. **Отчет о работе участка паросилового хозяйства за сутки**
4. **Отчет о работе электроремонтного цеха за сутки**
5. **Полный отчет за сутки** (доступен только администратору)
6. **Ретроспектива аварийных ремонтов за месяц** (Beta)

## Аутентификация

Для доступа к формам отчетов используются следующие учетные данные:

### Теплоэлектроцентраль
- Логин: `teplo_user`
- Пароль: `teplo_pass`
- Администратор: `admin` / `admin`

### Участок сточных вод
- Логин: `stok_user`
- Пароль: `stok_pass`
- Администратор: `admin` / `admin`

### Паросиловое хозяйство
- Логин: `paro_user`
- Пароль: `paro_pass`
- Администратор: `admin` / `admin`

### Электроремонтный цех
- Логин: `elektro_user`
- Пароль: `elektro_pass`
- Администратор: `admin` / `admin`

### Полный отчет
- Администратор: `admin` / `admin`

## Работа с приложением

### 1. Заполнение формы

**Шаг 1: Личные данные**
- ФИО (обязательно)
- Звание (обязательно)
- Должность (обязательно)
- Подразделение (обязательно)

**Шаг 2: Служебная информация**
- Дата рапорта (обязательно)
- Тип службы (обязательно)
- Время заступления и окончания
- Место несения службы

**Шаг 3: Дополнительная информация**
- Происшествия и особые отметки
- Состояние оборудования
- Погодные условия
- Приложения (файлы)
- Дополнительные замечания

### 2. Сохранение и восстановление

- **Автосохранение**: происходит автоматически через 1.5 секунды после изменения полей
- **Ручное сохранение**: кнопка "Сохранить черновик"
- **Восстановление**: при повторном открытии приложения предлагается восстановить незавершенный черновик

### 3. Предварительный просмотр и экспорт

- После заполнения всех обязательных полей нажмите "Предпросмотр"
- Проверьте корректность данных
- При необходимости вернитесь к редактированию
- Экспортируйте документ в PDF, Word или сохраните в формате JSON

### 4. Полный отчет

- Доступен только для администратора
- Позволяет объединить отчеты от всех подразделений в один документ
- Поддерживает экспорт в PDF

### 5. Ретроспектива аварийных ремонтов

- Функция позволяет просмотреть все аварийные ситуации за выбранный период
- Автоматически объединяет данные из всех отчетов
- Сортирует записи в хронологическом порядке
- Поддерживает экспорт в PDF

## Технические особенности

### Хранение данных

- **localStorage**: для небольших форм (до ~5MB)
- **IndexedDB**: автоматическое переключение при превышении лимитов
- **Файлы**: конвертируются в base64 и сохраняются локально

### Совместимость

- **Браузеры**: Chrome (рекомендуется), Edge, Firefox, Safari
- **Устройства**: настольные компьютеры, планшеты
- **ОС**: Windows, macOS, Linux

### Безопасность

- Все данные сохраняются только локально на устройстве пользователя
- Нет передачи данных на внешние серверы
- Автоматическая очистка старых черновиков (настраивается)

## Устранение неполадок

### Проблемы с сохранением

1. Проверьте, что в браузере включено локальное хранилище
2. Освободите место на диске
3. Проверьте настройки приватности браузера

### Проблемы с PDF

1. Убедитесь, что браузер поддерживает современные веб-технологии
2. Отключите блокировщики рекламы/скриптов
3. Попробуйте обновить страницу и повторить экспорт

### Проблемы с файлами

1. Проверьте размер загружаемых файлов (рекомендуется до 10MB)
2. Используйте поддерживаемые форматы: JPG, PNG, PDF, DOC, DOCX
3. При больших файлах приложение автоматически переключится на IndexedDB

## Дополнительные возможности

### Тестирование

В папке `test_reports` находятся тестовые отчеты для каждого подразделения:

- `test_report_tec.json` - Тестовый отчет для Теплоэлектроцентрали
- `test_report_zsv.json` - Тестовый отчет для Участка сточных вод
- `test_report_pcx.json` - Тестовый отчет для Паросилового хозяйства
- `test_report_erc.json` - Тестовый отчет для Электроремонтного цеха

Для тестирования откройте `test_reports/index.html` в браузере.

### Горячие клавиши

- `Tab` / `Shift+Tab`: навигация по полям
- `Enter`: переход к следующему шагу (при валидной форме)
- `Escape`: закрытие модальных окон

### Экспорт/импорт черновиков

В адаптере хранения предусмотрены методы для экспорта и импорта данных:

```javascript
// Экспорт всех черновиков
const exportData = await window.storageAdapter.exportData();

// Импорт черновиков
await window.storageAdapter.importData(jsonData);
```

### Очистка старых данных

Автоматическая очистка черновиков старше 30 дней:

```javascript
await window.storageAdapter.cleanupOldDrafts(30);
```

## Разработка и модификация

### Добавление новых полей

1. Добавьте поле в соответствующий шаг в `index.html`
2. Обновите валидацию в методе `validateStep()` в `app.js`
3. Добавьте отображение в методе `generatePreview()` в `app.js`

### Изменение стилей

Основные настройки находятся в начале файла `styles.css`:

- Цвета: переменные CSS для легкой кастомизации
- Размеры: адаптивные единицы для различных экранов
- Анимации: настраиваемые transition и animation

### Добавление новых шагов

1. Добавьте новый шаг в HTML с атрибутом `data-step`
2. Обновите `maxSteps` в `app.js`
3. Добавьте соответствующий элемент в прогресс-бар

## Лицензия

Свободное использование для личных и коммерческих целей.

## Поддержка

При возникновении вопросов или проблем:

1. Проверьте консоль браузера (F12) на наличие ошибок
2. Убедитесь, что все файлы проекта находятся в одной папке
3. Попробуйте открыть приложение в режиме инкогнито

---

**Версия**: 0.2.9  
**Последнее обновление**: 26 октября 2025 г.  
**Совместимость**: Chrome 88+, Edge 88+, Firefox 85+, Safari 14+